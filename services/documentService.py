# works with calling functions for preprocessing of added data, 
# scripts run when content is added to the document repository 
# (for dynamic indices, new document data is added to existing indices, barrels and lexicon)

